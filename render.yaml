services:
  - type: web
    runtime: static
    name: persona-fe
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
